const CustomError = require('../errors/CustomError');
const errorCodes = require('../errors/code');

const groupQuestionDao = require('../daos/groupQuestion');

const getAllGroupQuestion = () => {};

module.exports = { getAllGroupQuestion };
